# jhu_software_concepts - Module 1

Requirements
- Python 3.10+
- pip

Setup
1) Change into the app folder:
   cd personal_website
2) Create and activate a virtual environment (optional but recommended):
   python -m venv venv
   source venv/bin/activate
3) Install dependencies:
   pip install -r requirements.txt

Run
1) Start the app:
   python run.py
2) Open in your browser:
   http://localhost:8080
   (The app runs on 0.0.0.0:8080 by default.)

Notes
- Screenshots are provided in `personal_website_screenshots.pdf`.
